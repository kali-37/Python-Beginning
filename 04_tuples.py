#tuples are the immutable data types in python , you cannot change it's value
# from lists we cannot change it's value as they are tuples
# list uses [] parethesis , where as tuples uses () parenthesis

t=(1,2,4,5)


# Printing the elements of tuples
print(t[1])
# t2=(1) # tuples with single elements can't be written as this but 
t2=(1,)
print (t2)

